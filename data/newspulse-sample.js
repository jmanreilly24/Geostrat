/* ============================================================================
   NEWS PULSE  —  starter (empty)
   ----------------------------------------------------------------------------
   The live GDELT news-mention geography is written to data/newspulse.geojson by
   the "Update news pulse" GitHub Action (every few hours). Until that first run,
   this layer is simply empty.
   ========================================================================== */
window.NEWSPULSE = { type: "FeatureCollection", features: [] };
